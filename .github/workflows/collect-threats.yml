name: Collect Threat Intelligence

on:
  # Run daily at midnight UTC
  schedule:
    - cron: '0 0 * * *'

  # Allow manual trigger with optional log level
  workflow_dispatch:
    inputs:
      log_level:
        description: 'Log level (debug, info, warn, error)'
        required: false
        default: 'info'
        type: choice
        options:
          - debug
          - info
          - warn
          - error

env:
  NODE_VERSION: '22'

permissions:
  contents: read

jobs:
  collect:
    name: Run Threat Intelligence Collection
    runs-on: ubuntu-latest
    # Allows collection plus sequential LLM enrichment at the 30 RPM Groq limit
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Validate LLM provider contracts
        run: npm run llm:contract

      - name: Validate LLM enrichment quality regression
        run: npm run llm:quality

      - name: Run collector pipeline
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          LLM_PROVIDER: ${{ vars.LLM_PROVIDER || 'auto' }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GROQ_MODEL: ${{ vars.GROQ_MODEL || 'openai/gpt-oss-20b' }}
          OLLAMA_URL: ${{ secrets.OLLAMA_URL }}
          OLLAMA_API_KEY: ${{ secrets.OLLAMA_API_KEY }}
          OLLAMA_MODEL: ${{ vars.OLLAMA_MODEL || 'llama3.1:8b' }}
          LLM_MIN_CONFIDENCE: ${{ vars.LLM_MIN_CONFIDENCE || '0.65' }}
          LOG_LEVEL: ${{ inputs.log_level || 'info' }}
        run: npm run collect

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: collection-logs-${{ github.run_id }}
          path: |
            *.log
          retention-days: 7
